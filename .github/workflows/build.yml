name: Create New Release

on:
  workflow_dispatch:
  push:
    branches:
      - main

permissions:
  contents: write

env:
  CLIENT_SECRET: ${{ secrets.CLIENT_SECRET }}
  SECRET_ID: ${{ secrets.SECRET_ID }}

jobs:
  build:
    runs-on: macos-14

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          submodules: true

      - name: Set up Xcode
        uses: maxim-lobanov/setup-xcode@v1
        with:
          xcode-version: '16.2'

      - name: Verify Xcode
        run: |
          xcodebuild -version
          xcodebuild -showsdks

      - name: Clean DerivedData
        run: rm -rf ~/Library/Developer/Xcode/DerivedData

      - name: Verify CI secrets
        run: |
          [ -n "$CLIENT_SECRET" ] && echo "CLIENT_SECRET OK" || echo "CLIENT_SECRET missing"
          [ -n "$SECRET_ID" ] && echo "SECRET_ID OK" || echo "SECRET_ID missing"

      - name: Resolve Swift Packages
        run: |
          xcodebuild \
            -project Tools-Kit.xcodeproj \
            -scheme Tools-Kit \
            -destination 'generic/platform=iOS' \
            -resolvePackageDependencies

      - name: Test (iOS Simulator)
        run: |
          xcodebuild test \
            -project Tools-Kit.xcodeproj \
            -scheme Tools-Kit \
            -destination 'platform=iOS Simulator' \
            -quiet

      - name: Archive (iOS ONLY)
        run: |
          mkdir -p build

          xcodebuild \
            -project Tools-Kit.xcodeproj \
            -scheme Tools-Kit \
            -configuration Release \
            -destination 'generic/platform=iOS' \
            -sdk iphoneos \
            -archivePath build/Tools-Kit.xcarchive \
            archive \
            ONLY_ACTIVE_ARCH=NO \
            CODE_SIGNING_ALLOWED=NO \
            CODE_SIGNING_REQUIRED=NO \
            CODE_SIGN_IDENTITY="" \
            AD_HOC_CODE_SIGNING_ALLOWED=YES \
            SKIP_INSTALL=NO

      - name: Package IPA
        run: |
          mkdir -p build/Payload
          if [ -d build/Tools-Kit.xcarchive/Products/Applications/Tools-Kit.app ]; then
            cp -R build/Tools-Kit.xcarchive/Products/Applications/Tools-Kit.app build/Payload/
            cd build
            zip -r Tools-Kit.ipa Payload
          else
            echo "Error: Tools-Kit.app not found in xcarchive"
            exit 1
          fi

      - name: Verify IPA
        run: |
          ls -la build/
          test -f build/Tools-Kit.ipa && echo "IPA created"

      - name: Create Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: build-${{ github.run_number }}
          name: Tools-Kit Build ${{ github.run_number }}
          files: build/Tools-Kit.ipa
          generate_release_notes: true
