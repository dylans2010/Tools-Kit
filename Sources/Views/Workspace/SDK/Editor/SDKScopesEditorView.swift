

import SwiftUI

struct SDKScopesEditorView: View {
    var body: some View {
        IDEScopesView()
            .navigationTitle("Scopes Editor")
            .navigationBarTitleDisplayMode(.inline)
    }
}
